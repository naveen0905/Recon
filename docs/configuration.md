# Configuration Reference

> Full content added in Task 7.4. Placeholder to establish doc structure.

Team configuration is YAML stored in Azure SQL (`team_configs` table).
See `ARCHITECTURE.md` for the full schema.

All `{{secret:KEY_NAME}}` values are resolved from Azure Key Vault at runtime.
